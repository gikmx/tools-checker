import Is from './is';
import Props from './props';

export { Is, Props };

/**
 * @module checker
 * @description Gives you tools and shortcuts to determine typings.
 */
export default {
    is: Is,
    props: Props,
};
