import Is from './is';
import Props from './props';

export { Is, Props };

/**
 * Gives you tools to determine the type of a value.
 * @module checker
 */
export default {
    is: Is,
    props: Props,
};
